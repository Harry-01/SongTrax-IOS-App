/**
 * The footer to the web application
 *
 * @component

 * @returns {JSX.Element} The rendered React component.
 */
const Footer = () => {
    return <footer className="page-footer"></footer>;
};

export default Footer;
